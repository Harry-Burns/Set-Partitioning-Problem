All three algorithms can be ran directly from their respective .py file. 

At the bottom of each .py file is the main loop to run all 30 trials, as well as the parameters used.
At the top of each file is the function for loading in the problem. 
In each problem I have also provided the actual optimals in order to stop further procesing once its been reached.

To run these, the package numpy is required.